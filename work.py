import socket
import argparse
import threading
import concurrent.futures
import json
import time

host = "37.99.26.101"
port = 80

# First version scan port
def scan_port(host, port):
    pass

print(scan_port(host, port))